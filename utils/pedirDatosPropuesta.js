import inquirer from 'inquirer';
import chalk from 'chalk';

// Responsabilidad: Recolectar datos interactivos para propuestas (SRP)
export async function pedirDatosPropuesta(clientes) {
  // Lógica para prompts de inquirer (implementada por ti)
  console.log(chalk.blue('Ingresando datos de la propuesta...'));
  // Ejemplo:
  // const respuestas = await inquirer.prompt([...]);
  // return { clienteId, descripcion, precio, plazo };
}